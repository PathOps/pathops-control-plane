apiVersion: v1
kind: Service
metadata:
  name: control-plane-postgres
  namespace: control-plane
spec:
  ports:
    - port: 5432
      targetPort: 5432
      name: postgres
  selector:
    app: control-plane-postgres